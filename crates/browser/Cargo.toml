[package]
name = "browser"
version.workspace = true
edition.workspace = true

[dependencies]
gtk = "0.18"
log.workspace = true
raw-window-handle = "0.6"
wry = { version = "0.55", features = ["devtools"] }
